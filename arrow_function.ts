function soma(x: number, y: number): number {
  return x + y;
}

const arrowFunction = (x: number, y: number) => {
  return x + y;
};

// const arrowFunctionOneParamsAndDirectlyReturn = valor => valor * 10; // bad code
